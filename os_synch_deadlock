########### sec 1 Introduction ###########
gfg - https://tvsmotor.udemy.com/course/operating-systems-online-course/learn/lecture/13215020#overview

### (1_1) Concept of Shard Memory ###
-------
|      |
|------|
| P1   |  PCB of P1      communication "shared memory"
|------|
|      |
|------|      
| p2   |  PCB of P2
|------| 
|      |
|------|
| CS   |
|------|    


P1 & P2 blocks containn PCB
P1 & P2 cannot access each other directly
They use one common shared memory at location 1000

### (1_2) Need for Synchronization Mechanism - 1 ###
High level ---> Assembly ---> Machine code

f1(){
---
--
cout++;        LOAD COUNT R1; _____control change       
---            INCR R1;
--             STORE R1 COUNT; 
--
}

f2(){
---
--
cout--;        LOAD COUNT R1; _____control change          
---            DECR R1;
--             STORE R1 COUNT; 
---
--
}

if Non premtive then ok - but if non preemptive then if go for io cpu will be idle and cpu efficiency will decerease
if premtive then problem

### (1_3) Need for Synchronization Mechanism - 2 ###
1) Non premtive
2) Synchronization mechanism

p1-1,2,3,4,5      | p2-1,2,3 	| p1-6,7
	load p1,inc     load p2,dec   str p1, str p2		(inconsistent data -1)

########### sec 2 Critical Section and Race Condition  ###########
gfg - https://tvsmotor.udemy.com/course/operating-systems-online-course/learn/lecture/13331498#overview

### (2_1) Critical Section and Race Condition Explained ###

race condition --->

f1(){
---
--
cout++;       /// CRITICAL SECTION ///  1) LOAD COUNT R1; _____control change       /// CRITICAL SECTION /// 	
---            							2) INCR R1;
--             							3) STORE R1 COUNT; 
--
}

f2(){
---
--
cout--;       /// CRITICAL SECTION ///  1) LOAD COUNT R1; _____control change       /// CRITICAL SECTION ///   
---            							2) DECR R1;
--             							3) STORE R1 COUNT; 
---
--
}

CRITICAL SECTION - in code where we access shared memory

one possible way of execution - load p1, load p2, p1 inc, p1 dec, str p1, str p2
	5 become 4
	5 become 6

	but should be 5 - one inc and one dec

	4 - p1-1 | p2-1 | p1-2,3 | p2-2,3
	
	6 - p1-1,2 | p2-1,2,3 | p1-3

	5 - p1-1,2,3 | p2-1,2,3      or 	p2-1,2,3 | p1-1,2,3


RACE CONDITION - depending on the order of execution we are getting different output 

### (2_2) Why Race Condition happens ###

wrong ans bcz we are accesing cs at same time 

process synchronization - no two process in critical section at same time

### (2_3) More on Critical Section and Non Critical Section ###

critical section ---> It is the part of program where share memory is accessed.

race condition ---> The order of execution of instructions of process defines the result produced.

		p1	|	P2
		
		CS	    NCS		ok
		NCS		CS		ok
		NCS		NCS		ok
		
		CS		CS		problem


### (2_4) Synchronization explained  ###
p1						p2	
                        
1) NCS                  1) NCS
2)                      2)
----------------        3)
(entry section)         ----------------
-----------------       (entry section)
          			    -----------------
3) CS
4)			            4) CS			
-----------------       5)			
(exit section)         -----------------
-----------------       (exit section)
5) NCS                 -----------------
6)                      6) NCS
7)

single ticket gate

how long it hold the process

synch mechanism - depending of entry and exit section there are many synch mechanism.

########### sec 3 Various conditions of synch mechanism  ###########
udemy - https://tvsmotor.udemy.com/course/operating-systems-online-course/learn/lecture/13331630#overview

### (3_1)  Basic Idea of Mutual Exclusion ###

mutual exclusion - a way of making sure that if one process is executing the CS, then other process cannot enter the CS
                   until the executing process comes out of the CS.

					no two process are inside the cs at same time.

1) mutual exclusion
2) progress
3) bounded waiting


### (3_2)  Basic Idea of progress ###

progress - a way of making sure that if a process is not inside the CS(ie it is in NCS), then it will not block other process from entering the CS.

### (3_3)  Basic Idea of bounded waiting ###

bounded waiting - a way of making sure that there wxists a bound (or limit) on the number of times other processes are allowed to enter the CS after 
				  a process has made a request to enter the CS and before that request is granted.

generally entry section written in whilae loop

########### sec 4 LOCK synchronization mechanism  ###########
udemy - https://tvsmotor.udemy.com/course/operating-systems-online-course/learn/lecture/13331686#overview

### (4_1) Synchronization with Lock variable and its disadvantage ###

initially lock = 0
-------
| NCS |
-------

while(lock!=0);        semicolon is very important      					1) LOAD LOCK R1				semicolon is very important
			------> if prempted here many y process can enter              	2) CMP R1, #0
	 lock=1																	3) JNZ step 1)
																			4) STORE #1, LOCK
-------
| CS |
-------

lock = 0                     1) STORE #0 LOCK 

-------
| NCS |
-------

lock ---> lock is a bolean variable which indicates no of process inside CS at any point of time

1) Mutula exclusion    - no

### (4_2) progress condition for lock variable ###

lock 0 ---> no of process inside CS = 0
lock 1 ---> no of process inside CS = 1

2) progress  - yes

### (4_3) Bounded Waiting Condition for Lock variable ###

3) bounded waiting - no

### (4_4) Features of Lock Synchronization ###

if a process express interst to execute cs then there should be limit/bound on the no of times other process can enter cs

      synch
  _____|_____     
busy      nonbusy


1) multi process solution
2) busy waiting time solution --------------- it is solved by sleep mode
3) implement at user mode
4) no hardware support

	/// LOCK VARIABLE ///
	MUTUAL EXCLUSION - NO
	PROGRESS		 - YES
	BOUNDED WAITING  - NO

########### sec 5 TSL synchronization mechanism  ###########
udemy - https://tvsmotor.udemy.com/course/operating-systems-online-course/learn/lecture/13331788#overview

### (5_1) Important : Basics of Assembly Code ###

JNZ - jump if zeroflag is not set


|_|_|_|_|
 z v s c

CMP A,B
  if A=B, z=1
  else z=0
  
  1) LOAD LOCK R1
  2) CMP R1, #0-----------------------------------compare r1 with 0 if true then set z flag to 1....................if lock 0 then z=1
  3) JNZ step 1)----------------------------------jump to 1) if z flag is not set...but in above z flag is set
  4) STORE #1, LOCK
-
| CS |   

if z=1 , we say z flag is set
else z flagi is not set

### (5_2) When Mutual Exclusion can be violated in Lock synchronization mechanism ###

load #5 r1			R1---5
load #3 r2			R2---3
-----------------------before going to other inst r1,r2 value must be saved somewhere so that when control comes back it gives correct result
add r1,r2
load r2,r3

R1---5, del5, 6				save 5,3 somewhere
R2---3, del3, 7

load #6 r1			R1---5, del5, 6
load #7 r2			R2---3, del3, 7
add r1,r2
load r2,r3


-------
| NCS |
-------

while(lock!=0)               1) LOAD LOCK R1______________control change me violated many process in cs
 lock=1                      2) CMP R1, #0________________control change me violated many process in cs
                             3) JNZ step 1)
                             4) STORE #1, LOCK
-------
| CS |
-------

lock = 0                     1) STORE #0 LOCK 

-------
| NCS |
-------

load inst then prempted
p1: 1 | p2: 1,2,3,4 | p1: 2,3,4 
r1=0      r1=0
z=1       z=1......................................both 1 so two process can be in cs

load,cmp inst then prempted
p1: 1,2 | p2: 1,2,3,4 | p1: 2,3,4 
r1=0      r1=0
z=1       z=1......................................both 1 so two process can be in cs



both p1, p2

### (5_3)  ###

1) LOAD LOCK R1      _____no
2) CMP R1, #0        _____no
3) JNZ step 1)       _____no
4) STORE #1, LOCK                    


move the 4 inst to (2) line

1) LOAD LOCK R1      _____no
2) STORE #1, LOCK    _____prem can happen not violate me
3) CMP R1,#0    
4) JNZ step 1)

p1: 1,2,3 | p2: 1,2,3,4			me violated
r1=0		r1=0
z=1			z=1

p1: 1,2,3,4 | p2: 1,2,...1			me not violated
r1=0		r1=1
z=1			z=0		


no prempetion btw inst (1) and int (4) 		

### 5_4 TSL Synchronization Mechanism Explained ###

TSL(Test and Set lock)
  1) TSL (LOCK)          |  entry section
  2) CMP R1, #0          |
  3) JNZ step 1);        |

  CS

  4) STORE #0 , LOCK
_________________________________________

TSL(LOCK){
  Boolean R1;
  R1 = LOCK;
  LOCK = 1;
  return (R1)
}

instruction set  - ( ADD, JUMP, LOAD, TSL)
h/w or platform dependent

### 5_5 TSL - Mutual Exclusion, Progress, Bounded Waiting ###

1) mutual exclusion  - yes
2) progress          - yes
3) bounded waiting   - no
4) hw dependent

### 5_6 Features of TSL Synchronization Mechanism ###

1) Busy waiting soln    - yes....dis advantage
2) multi process soln   - yes....advantage
3) hw support necessary - yes....disadvantage 
4) implement in user mode

########### sec 6 problems with TSL ###########
udemy - https://tvsmotor.udemy.com/course/operating-systems-online-course/learn/lecture/13331920#overview

### 6_1 Checking of Starvation Problem ###

The entercs() and leavescs() functions to implement the critical section of a process are realised using test and set (TSL)
instruction as follows:

  void enters(x)
  {
      while(testandset(x));--------------check semicolon ....return initial value of lock, if 1 the infinite loop, if 0 enter cs
  }

  CS

  void leaves(x)
  {
      x=0;
  }

x is initialised to 0 which of the following is true?
A) the solution is starvation free
B) process enter cs in FIFO order
C) more than 1 process can enter the CS at the same time
D) the above solution is deadflock free

Boolean testandset(x)
{
    Bolean R1;
    R1=x;
    x=1;
    rerturn R1;...............why it is written ? use the tsl inside while() loop...return initial value of lock
}

### 6_2 Checking of Starvation Problem ###


P1, P2, P3....P500
_________________>500 

not in fifo order so B) is false
if process enter cs in fifo order then there will be no starvation

### 6_2 Checking of Starvation Problem ###
deadlock circular wait
if lock=0 whoever request first will enter cs

########### sec 7 Strict alteratin syschronization mechanism ###########
udemy - https://tvsmotor.udemy.com/course/operating-systems-online-course/learn/lecture/13332126#overview

### 7_1 Strict Alternation Synchronization - Mutual Exclusion, Progress ###

only two process

at atart turn can be 0 or 1

while(1)											|	while(1)																			
{                                                   |    {   
													                          |		 NCS	
    while( turn==1 )       // entry section         |        while( turn==0 )       // entry section
                                                    |    
    CS                                              |        CS
                                                    |    
    turn = 1;                                       |       turn = 0;
}                                                   |   }
    
	
progress - progress is not granted as p2 in ncs can block p1 to enter cc as turn = 1
	
### 7_2 Strict Alternation - Bounded Waiting ###
bounded waiting - yes

one after another

### 7_3 Features of strict alteration synchronization mechanism ###

1) busy waiting soln
2) 2 process soln
3) h/w or platform independent
4) implement in user code

////// STRICT ALTERATION METHOD //////
		MUTUAL EXCLUSION - YES
		PROGRESS		 - NO
		BOUNDED WAITING  - YES
		
		BUSY WAITING	 - YES	
		HW INDPT		 - YES
		2 PROCESS		 - YES   MANY PROCESS NO
		
		IMPLEMENT USER MODE  - YES	

### 7_4 Features of Strict Alternation Synchronization Mechanism ###
consider the methods used by process P1 and P2 for accesing their critical sections whenever needed as given below.
The initial values of shared boolean variables , S1 and S2 are raindable assigned.

method used by P1      |     methos used by p2

while(s1 == s2)       |      while( s1 != s2 )

CS                            CS

s1=s2                 |    s1 != s2


s1  |  s2
0		0----p2
0		1----p1
1		0----p1
1		1----p2


what of the following is true/
a) guarantee ME but not progress
b) guarantee progress but not ME
c) guarantee both ME and progress
d) guarantee neither ME and progress

########### sec 8 Disable Interrrupts Synchronization Mechanism ###########
udemy - https://tvsmotor.udemy.com/course/operating-systems-online-course/learn/lecture/13332126#overview

### 8_1 Disabling Interrupts Synchronization Explained Completely ###

while( 1 )
{
	disable interupts
	CS
	enable interupts
}


me				yes
progress		yes
bounded waiting yes


user can forget to enable interrupt...so process can run after this

### 8_2 Features of Disabling Interrupts and its disadvantage ###
	hw dependence
	user mode----no need to call system call
	
	user prog - can interrupt any time
	kernel prog-system call-sleep() - no interrupt

########### sec 9  interested Variabls Synchronization Mechanism ###########
udemy - https://tvsmotor.udemy.com/course/operating-systems-online-course/learn/lecture/13332192#overview

### 8_1 Interested Synchronization Mechanism Explained ###

Interested variables
Instead of one variable in turn variable synch mechanism, here we use 2 variables:
	Interested 0 = T or F			| Interested i indicates wheather process , Pi is interested in 
	Interested 1 = T or F			| in entering the CS or not ( i can take value 0 or 1 )
	

P0											
while( 1 )								|	while( 1 )					
{                                       |   {                             
	NON CS                              |   	NON CS                    
----------------------              	|   ----------------------                         
	Interested 0 = T					|   	Interested 0 = T		
	while( Interested = T)          	|   	while( Interested = T)    
----------------------					|   ----------------------						

	CS                                  |   	CS   

----------------------             	 	|   ---------------------- 									|   							
	Interested 0 = F                    |   	Interested 0 = F          
----------------------              	|   ----------------------                                     |    
}                                       |   }

### 8_2 Interested Synchronization Mutual Exclusion ###
Mutual Exclusion - yes

### 8_3 Interested Synchronization - deadlock ###

P0											
while( 1 )								|	while( 1 )					
{                                       |   {                             
	NON CS                              |   	NON CS                    
----------------------              	|   ----------------------                         
	Interested 0 = T___________intrpt	|   	Interested 0 = T	___________intrpt		both interept so both became deadlock	
	while( Interested = T)          	|   	while( Interested = T)    
----------------------					|   ----------------------						

	CS                                  |   	CS   

----------------------             	 	|   ---------------------- 									|   							
	Interested 0 = F                    |   	Interested 0 = F          
----------------------              	|   ----------------------                                     |    
}                                       |   }


### 8_4 Interested Synchronization - progress ###
If no process is inside the CS and some processes wish to enter the CS, then the decision on which process to enter the CS
should only be taken by processes which are not in NCS and this decision should not be postponed indefinitely.

 If a process is in NCS, then it shouldnot block other process from entering the CS.
 
 p1,p2 interested p3 not interested is in non-cs
 p1,p2 should decide who will enter cs it should not be decided by other process

if ... deadlock ---> progress___no


p0									|		p1								
while( 1 )                          |       while( 1 )                     
{                                   |       {                              
	NON CS                          |       	NON CS                     
---------------------------         |       ---------------------------    
	while ( turn == 1)              |       	while ( turn == 0)         
---------------------------         |       ---------------------------    
	                                |       	                           
	CS                              |       	CS                         
                                    |                                      
---------------------------         |       ---------------------------    
	turn = 1                        |       	turn = 0                  
---------------------------			|       ---------------------------		
	NON CS							|       	NON CS						
									|       								
}									|       }								

### 8_5 Interested Synchronization - progress continued ###

Interested

### 8_6 Interested Synchronization - bounded waiting ###

Mutual Excution -	yes 
Progress		-	no
Deadlock		-	yes
bounded waiting -   yes
hw dependence	-   no

if deadlock then no progress

########### sec 10  Peterson Synchronization Mechanism ###########
udemy - https://tvsmotor.udemy.com/course/operating-systems-online-course/learn/lecture/13332240#overview

### 10_1 Peterson Synchronization Mechanism intro ###


Peterson Method = ( turn + Interested )
--------------------------------GLOBAL variable--------------
Interested[ 0 ] = F;
Interested[ 1 ] = F;
int turn
___________________________________

void Entry_section( int process )			entry section
{
	int other = 1 - process;
	Interested[ process ] = T;
	turn = process;
	while( Interested[ other ] == T
				&&
			turn = process )
}
___________________________________
		CS
___________________________________
void Exitsection( in process )
{
	Interested[ process ] = F;
}


### 10_2 Difference between Local and Global Variables ###

void Entry_section( int process )			entry section
{

1)	int other = 1 - process;
2)	Interested[ process ] = T;
3)	turn = process;
4)	while( Interested[ other ] == T
				&&
			turn = process )
}
___________________________________
		CS
___________________________________
void Exitsection( in process )
{
1)	Interested[ process ] = F;
}
----------------------------------------------------	
P0
entry_section(0)			entry_section(1)

CS							CS

exit_section(0)				exit_section(1)

process=0					process=1
other=1						other=0


### 10_3 Peterson Synchronization Mechanism Explained ###


### 10_4 Peterson Synchronization Mechanism Continued ###

p0 1,2 | p1 1,2,3 | p0 3,4,4 | p1 4 

### 10_4 Peterson Synchronization Mechanism - Mutual Exclusion ###

Mutual Exclusion - yes

### 10_5 Peterson Synchronization Mechanism - Progress ###

progress - if no process is inside the CS and some processes wish to enter the CS, then the decision on which process
to enter the CS should be taken by processes which are not in NCS and that decision should not be postponed indefinitely.

	I[ 0 ] ] = F
	I[ 1 ]	= F

### 10_6 Peterson Synchronization Mechanism - Bounded Waiting ###

p0: 1,2,3,4, CS | p1: 1,2,3,4,4,4 | p0: 1,2,3,4 | p1:

### 10_7 Features of Peterson Synchronization Mechanism ###

Adv:
1. 
2. No H/W
3. user mode

Disadv:
1. 2-process
2. Busy waiting solution

########### sec 11  Problems on Non busy waiting synchronization techniques ###########
udemy - https://tvsmotor.udemy.com/course/operating-systems-online-course/learn/lecture/13332420#overview

### 11_1 Disadvantages of Busy Waiting Synchronization Techniques ###

disadvantage of busy waiting synch mechanish
	1) wastes CPU time
	2) priority inversion pblm
			only if we use priority.

### 11_2 problem 1 ###

consider peterson's algorithm for mutual exclusion b/w 2 concurrent processes, i and j. The program executed by process i is shown below:

	repeat
		flag[ i ] = TRUE
		turn = j
		while( p )

		CS

		flag[ i ] = FALSE

for the program to guarantee mutual exclusion, the predicate P in the while loop should be ___

A) flag[ j ]= TRUE and turn = i	----X
B) flag[ j ]= TRUE and turn = j	----
C) flag[ j ]= TRUE and turn = i	----X
D) flag[ j ]= TRUE and turn = i	----X

while( flag[j]==TRUE and turn==j)

	turn = i

### 11_3 problem 2 ###

two processes X and Y need to access a critical section consider the following synchronization construct used by both the processes

process X
	while( true )
	{
		varX = TRUE
		while( varY==TRUE)
		{
			CS
			varX=FALSE
		}
	}
____________________________________________________
process Y
	while( true )
	{
		varY = TRUE
		while( varX==TRUE)
		{
			CS
			varX=FALSE
		}
	}

### 11_4 Introduction to Non Busy Waiting Synchronization Techniques ###

Non Busy waiting synchronization mechanism
|
sleep_wakeup
|
block_wakeup
p1									|	p1								
while( p2 is inside CS )            |   while( p1 is inside CS )        
--------------------------          |   --------------------------      
	CS                              |   	CS                          
_______________________________     |   _______________________________ 
if( p2 is inisde CS)                |   if( p1 is inisde CS)            
	{                               |   	{                           
		sleep()                     |   		sleep()                 
	}                               |   	}                           
--------------------------			|   --------------------------		
	CS                              |		CS                          
--------------------------          |   --------------------------      
									|   								
wakeup(p2)                          |   wakeup(p1)                      
									|   								
									
p1
p2
p3___ready
p4
p5___blocked		

########### sec 12  Peterson Synchronization Mechanism ###########
udemy - https://tvsmotor.udemy.com/course/operating-systems-online-course/learn/lecture/13332474#overview

### 12_1  Producer Consumer Problem ###

# define N 10			// total no of slots in buffer
# define count 0		// no of slots filled in the buffer

void producer()							|	void consumer()						
{								        |   {								    
	int nums;                           |   	int nums;                       
	while( 1 )                          |   	while( 1 )                      
	{                                   |   	{                               
		num = produced_num();           |   		       
		                                |   		                            
		if( count == N)                 |   		if( count == N)             
			sleep();                    |   			sleep();                
		                                |   		                            
		insert_num(num) 	            |   		num = remove_num()	        
		                                |   		                            
		count = count + 1               |   		count = count - 1           
										|   									
		if(count == 1)                  |   		if(count == N-1)              
			wakeup(consumer)            |   			wakeup(prooducer)        
	                                    |   	                                
	}                                   |   	}                               
                                        |                                       
}                                       |   }                                   

### 12_1  Producer Consumer Problem Continued ###
### 12_3  Producer Consumer Problem Continued ###



########### sec 13  Binary semaphore ###########
udemy - https://tvsmotor.udemy.com/course/operating-systems-online-course/learn/lecture/13332590#overview

### 13_1  Basic idea of Structure in C programming ###

int a;
float b;

struct node {
	int a;
	float b;
};
struct node S;
_______________________________________________
struct semaphore{
	boolean value;
	QueueType L;
}

S.value
S.L


### 13_2  Binary Semaphores Explained 1 ###

while(1){
	wait(S);
		CS
	signal(S);
}
__________________________________________
wait(semaphore S){

	if(S.value == 1)
		S.value = 0;
	else{
		put the process's PCB in S.L;
		sleep();
	}
}
__________________________________________
signal(semaphore S){

	if(S.L is empty)
		s.value = 1
	else{
		select a process from S.L;
		wakeup();
	}
}

### 13_3  Binary Semaphores Explained 2 ###
	Binary semaphore (mutex )
	counting semaphore( mutex )


### 13_4 Wait () and Signal () System Calls ###


### 13_5 Wait and Signal synchronization mechanism - ME, Progress, Bounded Waiting ###

Mutual Exclusion - yes
progress		 - yes
bounded waiting	 - yes


### 13_6 Features of Wait and Signal Synchronization ###

multiprocess - yes
kernel mode	 - 
h/w independence - yes

### 13_7 Other names for Wait and Signal System Calls ###

wait()
P()
down()



signal()
V()
up()

binary semaphore or mutex
counting semaphore

########### sec 14  problems on binary semaphore  ###########

########### sec 15  Dining philosopher problem  ###########

### 15_1  Problem based on Dining Philosopher Problem ###

let m[0], m[1],...m[3] be mutexes and p[0], p[1], p[3] be processes.
each process p[i] executes:

	
	while(1)
	{
		down( m[i] );
		down( m[(i+1) % 4]);
		
		CS
		
		
		up( m[i] );
	    up( m[(i+1) % 4]);
	}



	p[0]				|	p[1]				|		p[2]				|		p[3]			
	while(1)			|	while(1)		    |        while(1)		    |        while(1)		
	{                   |   {                   |        {                  |        {                
		wait(m[0]);     |   	wait(m[1]);     |        	wait(m[2]);     |        	wait(m[3]);  
		wait(m[1]);     |   	wait(m[2]);     |        	wait(m[3]);     |        	wait(m[0]);  
		                |   	                |        	                |        	             
		CS              |   	CS              |        	CS              |        	CS           
		                |   	                |        	                |        	             
		signal(m[0]);   |   	signal(m[1]);   |        	signal(m[2]);   |        	signal(m[3]);
		signal(m[1]);   |   	signal(m[2]);   |        	signal(m[3]);   |        	signal(m[0]);
	}                   |   }                   |        }                  |        }                
	

### 15_2  Trick to avoid Deadlock ###
	
### 15_3  Dining Philosopher Problem Explained ###

let m[0], m[1],...m[3] be mutexes and p[0], p[1], p[3] be processes.
each process p[i] executes:

	
	while(1)
	{
		down( m[i] );
		down( m[(i+1) % 4]);
		
		CS
		
		
		up( m[i] );
	    up( m[(i+1) % 4]);
	}

dining philosopher problem
		PO
	M1	|	Mo
	|---------|
P3 -|  Rice	  |	-P1	
    |---------|
  M2	|	   M3
	    P2


Pi
1) Take the left fork ( M_i )
2) Take the right fork ( M_i+1%4  )
3) EAt
4) Drop the left fork ( M_i)
5) Drop the right fork ( M_i+1%4 )

### 15_4  Another Variation of Dining Philosopher Problem ###


4 process p[0], p[1]...p[3]
4 mutexes m[0], m[1]...m[3]

while( 1 )
{
	down( m[i+1]%4 );
	down( m[i] );

	CS

	up( m[i] )
	up( m[i+1]%4 )

}

p[0]	p[1]	p[2]	p[3]
|		|		|		|
|		|       |       |
|		|       |       |
|		|       |       |
m[0]	m[1]	m[m[2]	m[m[3]

########### sec 16  Counting semaphore  ###########

### 16_1  Introduction to Counting Semaphore ###

struct semaphore {
	int value;
	Queuetype L;
}

value - 3
L	  -


down(s)
----------
CS	p1,p2,p3
----------
up(s)

	semaphore
________|_______
|				|
binary		counting
semaphore	semaphore
(mutex)

### 16_2  Down() System call for Counting Semaphore ###

down( semaphore S )
{
	s.value = s.value - 1;
	if( s.value < 0)
	{
		put process's PCB in S.L;
		sleep();
	}
}

if s.value = +n
	it means n processes can enter CS further and 0 processes are waiting in the queuee of S
if s.value = -n
	it means 0 processes can enter the CS further and n processes are waiting in the queue of S
if s.value=0
	it means 0 processes can enter the CS further and 0 processes are waiting in the queue of S

### 16_3  Down System call for Counting Semaphore ###

### 16_4  Up() System call for Counting Semaphore ###

LIFO queue
FIFO queue
### 16_5  Example to illustrate Up() and Down() ###

S.value = 3,2,1,0,-1,-2,-1,0,1

a counting semaphore was initialized to 12. Then 8 wait and 5 signal operations were performed on this semaphore.
what will be the value of this semaphore variable after performing these operations?
	12-8+5

s.value=15, 17p and 3v operations performed?
	15-17+3 = 1


### 16_5  Example to illustrate Up() and Down() ###

let S be the counting semaphore variable. let p and v operations of counting semaphore are implemented using two binary semaphores xb and yb
and wait and signal operations of these binary semaphores.
the wait and signal operations of binary semaphores are represented by pb() and vb() respectively.

p(s)				|	p(s)					
pb(xb)              |   pb(xb)              
s=s-1               |   s=s-1  
					|
if(s<0)             |   if(s<0)             
	{               |   	{               
		vb(xb)      |   		vb(xb)      
		pb(yb)      |   		pb(yb)      
	}               |   	}               
else{               |   else{               
	vb(xb)			|   	vb(xb)			
}					|   }					
					|   					
					|   					

p1			p2

p(s)		p(s)
	CS			CS
v(s)	    v(s)	


########### sec 17  Deadlock  ###########

### 17_1  Deadlock and Starvation ###
Resource : anyphysical or virtual component in a computer ( which is available in limited quantity)
Deadlock : A situation in which two or more processes are unable to proceed because rach one of them is waiting
		   for one of the others to do something
starvation: a situation in which a runnable process is overlocked indefinitely by the scheduler.


p1--->rewust(OS)---> Acquires 2 uses-------------------> releases resources
				 |		 |								|
				 |		 | resources become free		|	
				 |--->	process blocked-----------------|

### 17_2 Necessary condition for Deadlock - Mutual Exclusion ###
1) mutual exclusion
2) hold & wait
3) no preemption of resource
4) circular wait

### 17_3 Necessary condition for Deadlock - Hold and Wait ###

### 17_4 Necessary condition for Deadlock - No Preemption ###

### 17_5 Necessary condition for Deadlock - Circular Wait ###

### 17_6 Minimum Number of resources needed so that deadlock can never happen ###

resources are of 2 types
	1) single instance resources
	2) multiple instance resources

a resource instance should be used by only one process at a time--->mutual exclusion

a system is having 4 user processes each requring 2 instances of resource 'R'. The minimum number of units of R such that no deadlock will occur is 


give (n-1) resources to every process. The will give the maximum no of resources needed such that deadlock can happen

give 1 more resource overall. this will give the minimum no of resirces needed such that deadlock will not happen


maximum no of resorces needed such that deadlock can happen

### 17_7 Problems ###
5 processes

each process needs 2 insrance of r. find minimum no of resources such that deadlock can never happen?

	P1	p2	P3	P4	P5
	|	|	|	|	|
	r1	r2	r3	r4	r5	r6


### 17_8 Maximum number of processes that can be present so that deadlock can be avoided ###


there are 5 units of resources and if each process pi requires 2 units to finish execution what is the maximum no of processes that can be system.
so that there will be no deadlock?

p = no of process
n = no of resources needed by single process
r1	r2	r3	r4
|	|	|	|
p1	p2	p3	p4


number of res = p(n-1)+1
number of res >= p(n-1)+1
			5 >= p(2-1)+1
			5 >= p+1 -> 4 >= p
				p <= 4


### 17_9 Problems ###
there are 18 units of resource and if each process pi requires 5 units to finish execution. what is the maximum no of processes that can bein the system
so that there will be no deadlock?

########### sec 18  Deadlock handling methods ###########

### 18_1   4 ways of handling deadlock ###

1) deadlock prevention
2) deadlock avoidance
3) deadlock ignorance
4) deadlock detection & recovery

### 18_1  Deadlock Prevention Techniques ###

disable one or more of the necessary conditions of deadlock


necessary conditions for deadlock
1) mutual exclusion
2) hold & wait
3) no preemption
4) circular wait

### 18_2  Deadlock Avoidance using Banker's Algorithm  ###

		allocation		| 	maximum
		x	| y	| z			x	| y	| z
p0		0	  0	  1			8	  4   3		
p1		3	  2   0			6     2   0 	
p2		2	  1	  1			3	  3	  3			


available
	x | y | z
p0  8	4	2
p1	3	0	0
p2	1	2	2

p0, p1, p2 - process
x, y, z    - resources


maximum -> maximum number of resources ( x, y, z) which are necessary to complete execution
allocation -> number of resources ( x, y, z) which are allocated to processes (p0, p1,p2) at this point of time













